1: DONE (2026-08-17) — Google OAuth + household groups shipped. Follow-ups spun out of that work,
   not yet built:
   1a: Household management UI — leave a household, remove a member, regenerate/revoke the
       invite code, rename a household, transfer ownership. Right now a household can only be
       created or joined, never managed afterward.
   1b: Redesign swipe_deck_shares' sharing UX for households bigger than 2 — today you pick one
       other member to send a deck to one at a time; worth deciding whether sharing should default
       to "the whole household" instead once groups get bigger.
   1c: Rate-limiting / abuse consideration — sign-up is now open to any Google account on a
       Supabase free-tier project sized for 2 users. Worth a look before this gets shared widely.
2: Extend the tinder mechanic to movies, tv shows, date night ideas, just about anything you want to decide on as a collective.
3: Add an "undo" for the last swipe - a quick way to reverse an accidental left/right/remove without having to hunt the recipe back down in a deck.
4: Let the household get notified when the other person shares a deck or the shopping list changes, instead of only finding out by opening the app and checking.
5: we need to adjust how the shopping list works. right now it feels cluttered and messy based on the large lists of ingredients we are pulling in.
6: DONE (2026-08-19) - ingredient sectioning fixed properly, in extract.py itself. The source
   EPUBs turned out to be on disk at C:\Users\Cameron\Documents\Vegan Cookbooks (61 books, 11GB)
   - an earlier note saying they were "no longer in the repo" was read as "lost", so the first
   attempt built a repair pass around damage the extractor could just be fixed to avoid. Both now
   exist: extract.py gets it right at source, repair_sections.py cleans up the residue.
   Result: section headers 2,416 -> 5,546, recipes with real sections 630 -> 1,824, empty section
   cards 515 -> 0, +1,032 ingredients into the ingredient and shopping lists, and zero recipe-id
   churn (nothing in Supabase was orphaned). Tags and decks rebuilt.
   What's still not fixed, and why:
   6a: 56 recipes whose title is a yield line ("SERVES 2", mostly Effortless Vegan). Now fixable
       in principle - the EPUBs are available - but the real name was never in the extracted
       text, so it needs new extractor logic, and fixing a title changes that recipe's id (sha1
       of book|file|title), which orphans its swipes/favorites/meal-plan rows. Cheap to do while
       those tables are near-empty; expensive once the household has been swiping for a while.
   6b: 767 partially-capsed titles ("Easy Red Lentil DAL") left alone - flattening them would
       wreck real acronyms.
   6c: 16 short label-shaped lines dropped corpus-wide by the extractor fix. 10 are genuine
       headings; 6 are numeral-less garnish items ("Your favorite gravy", "Lettuce and/or onion")
       that had previously been misfiled as method *steps*, so they were already in the wrong
       place. Recoverable by widening AMOUNT_WORD in extract.py if it ever matters.
   6d: Method headings ("FOR THE CHOCOLATE LAYER" above its step) are dropped rather than shown.
       Prepending each to its step would make the method much more readable in the ~200 recipes
       that have them - deliberately left out of scope, since it rewrites user-visible step text.
